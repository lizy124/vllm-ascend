# 交接文档：Layerwise 池化优化前基线（2026-09-07）

> 给接手者：本任务在 **192.168.13.165（容器 pool_165）** 上推进遇阻（layerwise 死锁），现交接给另一服务器继续。本文件是唯一入口，先读它 + [stage_summary_20260907.md](stage_summary_20260907.md) + [01_method.md](01_method.md) + 上级目录 [07_colleague_test_data.md](../07_colleague_test_data.md)。

---

## 1. 任务目标（一句话）

采 **DeepSeek-V4-Flash 在"未做本课题 layerwise 优化"时的性能基线**（TPS/TTFT/命中率，单/多前缀），作为之后 layerwise 优化后的对照。

## 2. 硬结论（已确认，别重复踩）

| # | 结论 | 证据 |
|---|---|---|
| C1 | **165 栈（vllm upstream 33e849499 + vllm-ascend 33e849499，缺 PR #15442/#15854）下 DP>1 必死锁**：`_sync_metadata_across_dp`（model_runner_v1.py:752）每次 execute_model 无条件跨 DP all_reduce，DP 间 step 错位即互等 | 2.1-2.4 节 |
| C2 | **layerwise（use_layerwise: true）在本栈必然死锁**（TP8×DP1 也死锁；prefetch 3/1 死锁，0 被代码强制拒绝 ≥1）——根因在 layerwise 按层加载机制使 rank 间前向节奏错位：rank A 卡 attention/MoE all_reduce 等 rank B，rank B 卡 `wait_for_layer_load`（pool_worker.py:1689）等 rank A 的 KV 保存（`wait_for_save_layer`） | 2.8/2.10/2.11 节 |
| C3 | **纯 KV 池（use_layerwise: false）跑通**：单请求 13s、32 并发 32/32（8s）——这是目前唯一可用的池化基线形态 | 2.9 节 |
| C4 | 同事口径（TP4×DP4 + 1M + MTP + prefetch=3）**在本栈不可复现**（C1+C2） | 07_colleague_test_data.md |
| C5 | TP16 不可行（`n_local_groups=0` 切分崩溃）；TP8 + 1M 不行（KV 内存不足 17.07G vs 11.82G，max_len 须 ≤ 187264） | 2.5/2.6 节 |

## 3. 环境速查

| 项 | 值 |
|---|---|
| 服务器 | 192.168.13.165，A3 × 16 卡（910B3，每芯 64G HBM） |
| 容器 | `pool_165`（editable 装 `/vllm-workspace`） |
| 权重 | `/mnt/weight/DeepSeek-V4-Flash-w8a8-mtp`（W8A8 + MTP draft） |
| KV 池服务 | memcache_hybrid MetaService（pid 94208，需存活）；大页 200000×2M；KV DRAM 16G |
| 本地脚本目录 | `D:\lzy\project\kv_pool\tmp\`（含 start_dsv4_layerwise.sh、verify_dp1.sh、launch_16c_v2.sh、dump*.sh 等） |
| 服务器中转 | `/home/lizhongyang/tmp/` → `docker cp` → 容器 `/home/lizhongyang/tmp/` |
| 服务日志 | 容器内 `/home/lizhongyang/map_165/run/dsv4_layerwise_v2_8100.log`（launch 前自动备份） |
| 本地代码 | `D:\lzy\project\kv_pool\code\vllm`、`vllm-ascend`（165 同源，可离线读代码） |

## 4. 当前状态（交接时刻）

- **服务已挂**：prefetch_layers=0 启动失败（`layerwise_prefetch_layers must be at least 1`，layerwise_cache_layout.py:147），20:53 EngineCore 崩溃退出，当前 165 上**无 vllm 服务在跑**。
- 已确认：layerwise（prefetch 3/1/0）均不可用 → **C2 成立**。165 目前唯一可跑形态 = **use_layerwise: false（纯 KV 池）**。
- stage_summary 已含全部 2.1-2.12 尝试与根因，**不要删除历史记录**。

## 5. 接手后建议路径（按优先级）

1. ~~确认 prefetch=0~~ 已确认（不可行）。**直接跳到决策基线形态**：
2. **决定基线形态**（二选一）：
   - **A（推荐，能立即产出）**：用 `use_layerwise: false` 跑通配置采"纯 KV 池基线"（单/多前缀 TPS/TTFT/命中率），作为对照基线 + 记录口径差异。
   - **B（治本）**：修复 layerwise 死锁后采真正的 layerwise 基线。修复方向见 §6。
3. **对齐同事口径**（治本且长期）：给 165 栈打 PR #15442 + #15854 补丁后恢复 TP4×DP4 + MTP + 1M + prefetch=3。**前提是拿到 PR 源码**（本地 `kv_pool/code/` 两树均为 33e849499，需从上游/同事处获取补丁）。

## 6. layerwise 死锁修复线索（给愿意深入的人）

- 关键代码：`vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/pool_worker.py`
  - L1673 `_submit_ready_layer_loads`：每层提交加载任务
  - L1689 `wait_for_layer_load`：主线程阻塞等本层 KV 加载
  - `LayerLoadTask(wait_for_save_layer=...)`：加载依赖源层保存 → **TP 下与 attention all_reduce 构成循环等待**
- 候选修法：
  - 加载完成前 attention 不 gate（去掉 `get_attention_compute_start_gate`），或加载改不阻塞主线程
  - 层间加 rank barrier 保证同步进入每层（牺牲重叠，换不锁死）
  - 对比 PR #15442/#15854 的 layerwise 改动（大概率官方已修）
- 验证手段：`py-spy dump`（容器内已装）看 EngineCore 卡 `acquire_read`、Worker 卡 `_moe_forward_shared`（MoE）即为死锁复现。

## 7. 关键命令速查

```bash
# 启动（含清理残留 + MetaService 检查）
bash /home/lizhongyang/tmp/launch_16c_v2.sh

# 冒烟验证（READY → idle → 单请求 → 32 并发）
bash /home/lizhongyang/tmp/verify_dp1.sh

# 死锁诊断（EngineCore / Worker 栈）
docker exec pool_165 bash /home/lizhongyang/tmp/dump_ec.sh
docker exec pool_165 bash /home/lizhongyang/tmp/dumpall8.sh

# 日志
tail -50 /home/lizhongyang/map_165/run/dsv4_layerwise_v2_8100.log
```

## 8. 注意事项

- **editable 代码冻结纪律**：改 `/vllm-workspace` 源码即改栈版本，基线对比会失效；任何修改先归档 `git rev-parse HEAD` + 改动 diff。
- 本地改脚本 → scp 到 165 `/home/lizhongyang/tmp/` → `docker cp` 进容器，别直接在容器里改（会丢）。
- 每次 launch 前会备份上一轮日志，历史日志按轮保留。
- KV 池相关：大页/DRAM 配置变更会影响 HalMemCreate（128G 会失败，用 16G）。
